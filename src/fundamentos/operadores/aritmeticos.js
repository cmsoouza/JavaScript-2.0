const [a, b, c, d] = [7, 3, 4, 2];

const soma = a + b + c + d;
const subtracao = d - b;
const multiplicacao = a * b;
const divisao = c / d;
const modulo = a % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo);

/**
 * comentarios;
 *  => Atraves da operação entre seus operandos, o resultado retorna unico valor numérico.
 *  => Operadores aritméticos:
 *      +: soma
 *      -: subtração
 *      *: multiplicação
 *      /: divisão
 *
 *  Mais sobre operadpres no link abaixo
 *  https://developer.mozilla.org/pt-BR/docs/Web/JavaScript/Guide/Expressões_Aritméticas
 */
